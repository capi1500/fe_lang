fn main() {
	const r = 1..10;
	var i = 0;
	while (i < length_i32(&r)) {
		print_i32(r[i]);
		print_string(" ");
		i += 1;
	}
	print_string("\n");
	const rev = 10..1;
	i = 0;
	while (i < length_i32(&rev)) {
		print_i32(rev[i]);
		print_string(" ");
		i += 1;
	}
	print_string("\n");
}